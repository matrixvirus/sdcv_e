0 介绍
	sdcv是stardict(星际译王）的命令行版，它的使用有些不放便的地方，它查了
	一个单词后，如果篇幅较长，你要用Shift+Up/Down 上下翻动，我封装了它，使用
	less命令查看每个单词的结果，所以你可以用j键下翻，k键上翻。
	你必须先安装了sdcv，并安装了字典文件。
	
1 用法
	直接执行
	./sdcv_e.sh

	或后面跟要查的单词比如boxer和clover两个单词
	./sdcv_e.sh boxer clover
